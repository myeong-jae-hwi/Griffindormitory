# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on PR
# on: pull_request
# permissions:
#   checks: write
#   contents: read
#   pull-requests: write
# jobs:
#   build_and_preview:
#     if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - run: npm run serve
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_GRYFFINDORMITOR }}
#           projectId: gryffindormitor

jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'  # 사용 중인 Node.js 버전에 맞게 설정

      - name: Install dependencies
        working-directory: ./griffin
        run: npm install

      - name: Build project
        working-directory: ./griffin
        run: npm run build

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT }}"
          channelId: preview
          projectId: "your-firebase-project-id"
          working-directory: ./griffin  # Firebase 명령어를 실행할 위치 설정
